death_timer=120;
with (instance_create_layer(view_xview[0] + (view_wview[0] / 2), view_yview[0] + 70, layer, text_object)) {
    // Set the text and calculate its length
    text = "MORISTE CAPO POR SI NO TE DISTE CUENTA";
    length = string_length(text);
}