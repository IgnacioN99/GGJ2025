function DrawSetText(color, font, halign, valign) {
    draw_set_color(color);        // Set the draw color
    draw_set_font(font);          // Set the font
    draw_set_halign(halign);      // Set horizontal alignment
    draw_set_valign(valign);      // Set vertical alignment
}